spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/grail?useSSL=true
    username: root
    password: root
  main:
    banner-mode: off

mybatis-plus:
  global-config:
    banner: false

grail:
  path: images/